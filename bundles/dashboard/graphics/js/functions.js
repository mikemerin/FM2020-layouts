class Layout {

  constructor() {
    this.name = this.getLayoutName()
  }

  getLayoutName = () => {
    // this.name = location.href.match(/(layout.*)(.html)/)[1];
    // var replicants = [];
    // ["mainInfo", "playerInfo"].forEach(fieldGroup => {
    //     fieldGroups[fieldGroup].fields.forEach(field => {
    //         var fieldSanitized = sanitize(field.fieldName);
    //         replicants.push(nodecg.Replicant(fieldGroup + "_field_" + fieldSanitized);
    //     })
    // })
    // console.log(Object.keys(replicants))
    // console.log(replicants)
    // NodeCG.waitForReplicants.apply(Object.keys(replicants)).then(() => {
    //     console.log("hit")
    //     Object.keys(replicants).forEach(replicant => {
    //         console.log(replicants[replicant])
    //     })
    // })
    // pick up with fieldGroups, getting all the replicants. Start with getting the layout name (layout_1_608), then get the other fields, then make a stylesheet for the layout against the locations
  }

}
